export type HappeningType = {
  date: string | number | Date;
  id: string;
  title: string;
  description: string;
  location: string;
  event_type: string;
  seats: string;
  price: string;
};

// export type AdminType = {

// }
